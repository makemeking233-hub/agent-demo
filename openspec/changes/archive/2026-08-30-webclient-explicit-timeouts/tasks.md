# Tasks: WebClient Explicit Timeouts

- [x] T1: OpenAiCompatibleProvider 加 responseTimeout + connectTimeout（默认值 60s/10s）→ DeepSeekProvider/MiniMaxProvider 透传 → 跑测试 + commit
- [x] T2: openspec archive webclient-explicit-timeouts
