/**
 * form.tsx placeholder (add-shadcn-infra 阶段).
 *
 * shadcn 4.x 输出的 form.tsx 是严重压缩的单行 TS，esbuild 与 swc 都无法解析；
 * 该文件被人工简化成一个 stub。完整 form 组件（react-hook-form + FieldProvider
 * 集成）将在 shadcn-components-p1 (§2) 装包时重新生成。
 */
export {}