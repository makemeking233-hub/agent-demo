/**
 * dropdown-menu.tsx placeholder (add-shadcn-infra 阶段).
 *
 * shadcn 4.x 输出严重压缩（行 12 含 `}) {` 无分号等），esbuild / swc 无法解析；
 * 该文件被人工简化为 stub。完整 dropdown-menu 组件将在 shadcn-components-p1 (§2)
 * 装包时从 registry 重新拉取并使用 prettier 格式化。
 */
export {}